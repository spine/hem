// Generated by CoffeeScript 1.4.0
(function() {
  var Dependency, Package, Stitch, compilers, eco, fs, npath, stitch, toArray, uglify;

  npath = require("path");

  fs = require('fs');

  eco = require('eco');

  uglify = require('uglify-js');

  compilers = require('./compilers');

  stitch = require('../assets/stitch');

  Dependency = require('./dependency');

  Stitch = require('./stitch');

  toArray = require('./utils').toArray;

  Package = (function() {

    function Package(config) {
      if (config == null) {
        config = {};
      }
      this.identifier = config.identifier;
      this.libs = toArray(config.libs || []);
      this.paths = toArray(config.paths || []);
      this.dependencies = toArray(config.dependencies || []);
      this.target = config.target;
      this.extraJS = config.extraJS || "";
      this.test = config.test;
    }

    Package.prototype.compileModules = function() {
      this.dependency || (this.dependency = new Dependency(this.dependencies));
      this.stitch = new Stitch(this.paths);
      this.modules = this.dependency.resolve().concat(this.stitch.resolve());
      return stitch({
        identifier: this.identifier,
        modules: this.modules
      });
    };

    Package.prototype.compileLibs = function() {
      var path;
      return ((function() {
        var _i, _len, _ref, _results;
        _ref = this.libs;
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          path = _ref[_i];
          _results.push(fs.readFileSync(path, 'utf8'));
        }
        return _results;
      }).call(this)).join("\n");
    };

    Package.prototype.compile = function(minify) {
      var result;
      try {
        result = [this.compileLibs(), this.compileModules(), this.extraJS].join("\n");
        if (minify) {
          result = uglify(result);
        }
        return result;
      } catch (ex) {
        if (ex.stack) {
          console.error(ex);
        } else {
          console.trace(ex);
        }
        return result = "console.log(\"" + ex + "\");";
      }
    };

    Package.prototype.compileSingle = function(path, parent) {
      var mods, result;
      if (path) {
        this.stitch = new Stitch([path]);
        this.modules = this.stitch.resolveFiles(parent);
        if (!this.modules.length) {
          return null;
        }
      } else {
        this.dependency || (this.dependency = new Dependency(this.dependencies));
        this.modules = this.dependency.resolve();
        mods = stitch({
          identifier: this.identifier,
          modules: this.modules
        });
        result = [this.compileLibs(), mods, this.extraJS].join("\n");
      }
      return stitch({
        identifier: this.identifier,
        modules: this.modules
      });
    };

    Package.prototype.unlink = function() {
      if (fs.existsSync(this.target)) {
        return fs.unlinkSync(this.target);
      }
    };

    Package.prototype.createServer = function() {
      var _this = this;
      return function(env, callback) {
        return callback(200, {
          'Content-Type': 'text/javascript'
        }, _this.compile());
      };
    };

    Package.prototype.createIServer = function(prefix) {
      var _this = this;
      return function(env, callback) {
        var path, result;
        console.log("hem server called with", env.pathInfo);
        if (env.pathInfo.slice(-3) === '.js') {
          path = env.pathInfo.slice(1, -3);
        } else {
          path = env.pathInfo.slice(1);
        }
        if (path === ("" + prefix + "/core")) {
          result = _this.compileSingle();
        } else {
          path = path;
          result = _this.compileSingle(path, npath.resolve(prefix));
        }
        if (result) {
          return callback(200, {
            'Content-Type': 'text/javascript'
          }, result);
        } else {
          return callback(400, {
            'Content-Type': 'text/javascript'
          }, 'No such file');
        }
      };
    };

    return Package;

  })();

  module.exports = {
    compilers: compilers,
    Package: Package,
    createPackage: function(config) {
      return new Package(config);
    }
  };

}).call(this);
