// Generated by CoffeeScript 1.6.2
(function() {
  var connect, createRoutingProxy, fs, http, httpProxy, mime, patchServerResponseForRedirects, server, utils;

  connect = require('connect');

  mime = require('connect')["static"].mime;

  http = require('http');

  fs = require('fs');

  utils = require('./utils');

  httpProxy = require('http-proxy');

  server = {};

  server.start = function(hem, options) {
    var app;

    app = connect();
    app.use(server.middleware(hem, options));
    server.setupStaticRoutes(app, hem, options);
    return http.createServer(app).listen(options.port, options.host);
  };

  server.middleware = function(hem, options) {
    return function(req, res, next) {
      var hemapp, pkg, str, url, _i, _len, _ref, _ref1;

      url = ((_ref = require("url").parse(req.url)) != null ? _ref.pathname.toLowerCase() : void 0) || "";
      if (url.match(/\.js|\.css/)) {
        _ref1 = hem.apps;
        for (_i = 0, _len = _ref1.length; _i < _len; _i++) {
          hemapp = _ref1[_i];
          if (pkg = hemapp.isMatchingRoute(url)) {
            str = pkg.build();
            res.charset = 'utf-8';
            res.setHeader('Content-Type', mime.lookup(pkg.target));
            res.setHeader('Content-Length', Buffer.byteLength(str));
            res.end((req.method === 'HEAD' && null) || str);
            return;
          }
        }
      }
      return next();
    };
  };

  server.setupStaticRoutes = function(app, hem, options) {
    var hemapp, pkg, route, value, _i, _j, _len, _len1, _ref, _ref1, _ref2, _results;

    _ref = hem.apps;
    for (_i = 0, _len = _ref.length; _i < _len; _i++) {
      hemapp = _ref[_i];
      if (utils.VERBOSE) {
        utils.log("> Apply route mappings for application: <green>" + hemapp.name + "</green>");
        _ref1 = hemapp.packages;
        for (_j = 0, _len1 = _ref1.length; _j < _len1; _j++) {
          pkg = _ref1[_j];
          utils.log("- Mapping route  <yellow>" + pkg.route + "</yellow> to <yellow>" + pkg.target + "</yellow>");
        }
      }
      if (hemapp["static"]) {
        options.routes = utils.extend(hemapp["static"], options.routes);
      }
    }
    _ref2 = options.routes;
    _results = [];
    for (route in _ref2) {
      value = _ref2[route];
      if (typeof value === 'string') {
        if (fs.existsSync(value)) {
          utils.verbose("- Mapping static <yellow>" + route + "</yellow> to <yellow>" + value + "</yellow>");
          _results.push(app.use(route, connect["static"](value)));
        } else {
          _results.push(utils.errorAndExit("The folder " + value + " does not exist."));
        }
      } else if (value.host) {
        utils.verbose("- Proxy requests <yellow>" + route + "</yellow> to <yellow>" + value.host + ":" + (value.port || 80) + value.hostPath + "</yellow>");
        _results.push(app.use(route, createRoutingProxy(value)));
      } else {
        _results.push(utils.errorAndExit("Invalid route configuration for <yellow>" + route + "</yellow>"));
      }
    }
    return _results;
  };

  createRoutingProxy = function(options) {
    var proxy;

    if (options == null) {
      options = {};
    }
    proxy = new httpProxy.RoutingProxy();
    options.hostPath || (options.hostPath = "");
    options.port || (options.port = 80);
    options.patchRedirect || (options.patchRedirect = true);
    if (options.patchRedirect) {
      proxy.once("start", function(req, res) {
        var returnHost;

        returnHost = req.headers.host;
        return patchServerResponseForRedirects(options.host, returnHost);
      });
    }
    return function(req, res, next) {
      req.url = "" + options.hostPath + req.url;
      return proxy.proxyRequest(req, res, options);
    };
  };

  patchServerResponseForRedirects = function(fromHost, returnHost) {
    var writeHead;

    writeHead = http.ServerResponse.prototype.writeHead;
    return http.ServerResponse.prototype.writeHead = function(status) {
      var headers, newLocation, oldLocation;

      if (status === 301 || status === 302) {
        headers = this._headers;
        oldLocation = new RegExp(":\/\/" + fromHost + ":?[0-9]*");
        newLocation = "://" + returnHost;
        headers.location = headers.location.replace(oldLocation, newLocation);
      }
      return writeHead.apply(this, arguments);
    };
  };

  module.exports = server;

}).call(this);
