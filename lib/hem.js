// Generated by CoffeeScript 1.3.3
(function() {
  var Hem, argv, compilers, css, fs, help, http, httpProxy, optimist, path, pkg, specs, strata;

  path = require('path');

  fs = require('fs');

  optimist = require('optimist');

  strata = require('strata');

  compilers = require('./compilers');

  pkg = require('./package');

  css = require('./css');

  specs = require('./specs');

  http = require('http');

  httpProxy = require('http-proxy');

  argv = optimist.usage(['  usage: hem COMMAND', '    server  start a dynamic development server', '    build   serialize application to disk', '    watch   build & watch disk for changes', '    test    build and run tests'].join("\n")).alias('p', 'port').alias('d', 'debug').argv;

  help = function() {
    optimist.showHelp();
    return process.exit();
  };

  Hem = (function() {

    Hem.exec = function(command, options) {
      return (new this(options)).exec(command);
    };

    Hem.include = function(props) {
      var key, value, _results;
      _results = [];
      for (key in props) {
        value = props[key];
        _results.push(this.prototype[key] = value);
      }
      return _results;
    };

    Hem.prototype.compilers = compilers;

    Hem.prototype.options = {
      slug: './slug.json',
      css: './css',
      libs: [],
      "public": './public',
      paths: ['./app'],
      dependencies: [],
      port: process.env.PORT || argv.port || 9294,
      host: argv.host || 'localhost',
      useProxy: argv.useProxy || false,
      apiHost: argv.apiHost || 'localhost',
      apiPort: argv.apiPort || 8080,
      proxyPort: argv.proxyPort || 8001,
      cssPath: '/application.css',
      jsPath: '/application.js',
      test: './test',
      testPublic: './test/public',
      testPath: '/test',
      specs: './test/specs',
      specsPath: '/specs.js'
    };

    function Hem(options) {
      var key, value, _ref;
      if (options == null) {
        options = {};
      }
      for (key in options) {
        value = options[key];
        this.options[key] = value;
      }
      _ref = this.readSlug();
      for (key in _ref) {
        value = _ref[key];
        this.options[key] = value;
      }
    }

    Hem.prototype.server = function() {
      var _this = this;
      this.removeOldBuilds();
      strata.use(strata.contentLength);
      strata.get(this.options.cssPath, this.cssPackage().createServer());
      strata.get(this.options.jsPath, this.hemPackage().createServer());
      if (fs.existsSync(this.options["public"])) {
        strata.use(strata.file, this.options["public"], ['index.html', 'index.htm']);
      }
      if (fs.existsSync(this.options.testPublic)) {
        strata.map(this.options.testPath, function(app) {
          app.get(_this.options.specsPath, _this.specsPackage().createServer());
        }
        if (path.existsSync(_this.options.testPublic)) {
          return app.use(strata.file, _this.options.testPublic, ['index.html', 'index.htm']);
        });
      }
      return strata.run({
        port: this.options.port,
        host: this.options.host
      });
    };

    Hem.prototype.removeOldBuilds = function() {
      var filePath, files, proxy, _i, _len,
        _this = this;
      files = [path.join(this.options["public"], this.options.jsPath), path.join(this.options["public"], this.options.cssPath), path.join(this.options.testPublic, this.options.specsPath)];
      for (_i = 0, _len = files.length; _i < _len; _i++) {
        filePath = files[_i];
        if (fs.existsSync(filePath)) {
          fs.unlinkSync(filePath);
        }
      }
      if (this.options.useProxy) {
        proxy = new httpProxy.RoutingProxy();
        return http.createServer(function(req, res) {
          var startsWithBasePath;
          console.log('just stock url', req.url);
          startsWithBasePath = new RegExp("^" + _this.options.baseSpinePath);
          if (startsWithBasePath.test(req.url)) {
            req.url = req.url.replace(_this.options.baseSpinePath, '/');
            return proxy.proxyRequest(req, res, {
              host: _this.options.host,
              port: _this.options.port
            });
          } else {
            return proxy.proxyRequest(req, res, {
              host: _this.options.apiHost,
              port: _this.options.apiPort
            });
          }
        }).listen(this.options.proxyPort);
      }
    };

    Hem.prototype.build = function(buildTests) {
      var source;
      if (buildTests == null) {
        buildTests = false;
      }
      source = this.hemPackage().compile(!argv.debug);
      fs.writeFileSync(path.join(this.options["public"], this.options.jsPath), source);
      source = this.cssPackage().compile();
      fs.writeFileSync(path.join(this.options["public"], this.options.cssPath), source);
      if (buildTests) {
        source = this.specsPackage().compile();
        return fs.writeFileSync(path.join(this.options.testPublic, this.options.specsPath), source);
      }
    };

    Hem.prototype.watch = function(callback) {
      var dir, lib, _i, _len, _ref, _results,
        _this = this;
      this.build();
      _ref = ((function() {
        var _j, _len, _ref, _results1;
        _ref = this.options.libs;
        _results1 = [];
        for (_j = 0, _len = _ref.length; _j < _len; _j++) {
          lib = _ref[_j];
          _results1.push(path.dirname(lib));
        }
        return _results1;
      }).call(this)).concat(this.options.css, this.options.paths, this.options.specs);
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        dir = _ref[_i];
        if (!fs.existsSync(dir)) {
          continue;
        }
        _results.push(require('watch').watchTree(dir, function(file, curr, prev) {
          if (curr && (curr.nlink === 0 || +curr.mtime !== +(prev != null ? prev.mtime : void 0))) {
            console.log("" + file + " changed.  Rebuilding.");
            return _this.build();
          }
        }));
      }
      return _results;
    };

    Hem.prototype.test = function() {
      return this.build(true);
    };

    Hem.prototype.exec = function(command) {
      if (command == null) {
        command = argv._[0];
      }
      if (!this[command]) {
        return help();
      }
      this[command]();
      switch (command) {
        case 'build':
          return console.log('Built application');
        case 'watch':
          return console.log('Watching application');
        case 'test':
          return console.log('Testing application');
      }
    };

    Hem.prototype.readSlug = function(slug) {
      if (slug == null) {
        slug = this.options.slug;
      }
      if (!(slug && fs.existsSync(slug))) {
        return {};
      }
      return JSON.parse(fs.readFileSync(slug, 'utf-8'));
    };

    Hem.prototype.cssPackage = function() {
      return css.createPackage(this.options.css);
    };

    Hem.prototype.hemPackage = function() {
      return pkg.createPackage({
        dependencies: this.options.dependencies,
        paths: this.options.paths,
        libs: this.options.libs
      });
    };

    Hem.prototype.specsPackage = function() {
      return specs.createPackage(this.options.specs);
    };

    return Hem;

  })();

  module.exports = Hem;

}).call(this);
