// Generated by CoffeeScript 1.6.3
(function() {
  var Hem, application, argv, compilers, fs, help, optimist, path, server, testing, utils,
    __indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  path = require('path');

  optimist = require('optimist');

  fs = require('fs');

  utils = require('./utils');

  compilers = require('./compilers');

  server = require('./server');

  application = require('./package');

  testing = require('./test');

  argv = optimist.usage(['usage:\nhem COMMAND', '    server  :start a dynamic development server', '    build   :serialize application to disk', '    watch   :build & watch disk for changes', '    test    :build and run tests', '    clean   :clean compiled targets', '    version :version the application files', '    check   :check slug file values'].join("\n")).alias('p', 'port').describe('p', ':hem server port').alias('c', 'compress').describe('c', ':all complications are compressed/minified').alias('w', 'watch').describe('w', ':watch files when running tests').alias('s', 'slug').describe('s', ':run hem using a specified slug file').alias('n', 'nocolors').describe('n', ':disable color in console output').describe('v', ':make hem more talkative(verbose)').argv;

  argv.command = argv._[0];

  argv.targets = argv._.slice(1);

  if (!!argv.nocolors) {
    require("sty").disable();
  }

  utils.ARGV = argv;

  utils.COMPRESS = argv.compress = !!argv.compress;

  utils.VERBOSE = argv.v = !!argv.v;

  utils.COMMAND = argv.command;

  help = function() {
    var _ref;
    utils.log("<b>HEM</b> Version: <green>" + ((_ref = require('../package.json')) != null ? _ref.version : void 0) + "</green>\n");
    optimist.showHelp();
    return process.exit();
  };

  Hem = (function() {
    Hem.exec = function(command, options) {
      return (new this(options)).exec(command);
    };

    Hem.include = function(props) {
      var key, value, _results;
      _results = [];
      for (key in props) {
        value = props[key];
        _results.push(this.prototype[key] = value);
      }
      return _results;
    };

    Hem.middleware = function(slugFile) {
      var hem;
      hem = new Hem(slugFile);
      return server.middleware(hem.apps, hem.options.server);
    };

    Hem.prototype.compilers = compilers;

    Hem.prototype.homeDir = '';

    Hem.prototype.options = {
      hem: {
        port: 9294,
        host: "localhost"
      }
    };

    Hem.prototype.apps = [];

    function Hem(options) {
      var config, name, slug, _base, _base1, _ref, _ref1, _ref2;
      if (options == null) {
        options = {};
      }
      if (options === "string") {
        slug = options;
      } else {
        slug = argv.slug || './slug.json';
        if (options) {
          this.options = utils.extend(options, this.options);
        }
      }
      if (fs.existsSync(slug)) {
        options = this.readSlug(slug);
        if ((_ref = options.hem) != null) {
          _ref.port || (_ref.port = this.options.hem.port);
        }
        if ((_ref1 = options.hem) != null) {
          _ref1.host || (_ref1.host = this.options.hem.host);
        }
        this.options = options;
        this.homeDir = path.dirname(path.resolve(process.cwd() + "/" + slug));
        process.chdir(this.homeDir);
      } else {
        utils.errorAndExit("Unable to find " + slug + " file in current directory");
      }
      if (argv.port) {
        this.options.hem.port = argv.port;
      }
      (_base = this.options.hem).host || (_base.host = "");
      (_base1 = this.options.hem).routes || (_base1.routes = {});
      _ref2 = this.options;
      for (name in _ref2) {
        config = _ref2[name];
        if (name === "hem") {
          continue;
        }
        config.hem = this.options.hem;
        this.apps.push(application.createApplication(name, config));
      }
    }

    Hem.prototype.server = function() {
      var value;
      value = "http://" + (this.options.hem.host || "localhost") + ":" + this.options.hem.port;
      utils.log("Starting Server at <blue>" + value + "</blue>");
      return server.start(this.apps, this.options.hem);
    };

    Hem.prototype.clean = function() {
      var app, cleanAll, targets, _i, _len, _ref, _ref1, _results;
      targets = argv.targets;
      cleanAll = targets.length === 0;
      _ref = this.apps;
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        app = _ref[_i];
        if ((_ref1 = app.name, __indexOf.call(targets, _ref1) >= 0) || cleanAll) {
          _results.push(app.unlink());
        }
      }
      return _results;
    };

    Hem.prototype.build = function() {
      this.clean();
      return this.buildTargets(argv.targets);
    };

    Hem.prototype.version = function() {
      return this.versionTargets(argv.targets);
    };

    Hem.prototype.watch = function() {
      var app, targets, watchAll, _i, _len, _ref, _ref1, _results;
      targets = argv.targets;
      this.buildTargets(targets);
      watchAll = targets.length === 0;
      _ref = this.apps;
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        app = _ref[_i];
        if ((_ref1 = app.name, __indexOf.call(targets, _ref1) >= 0) || watchAll) {
          _results.push(app.watch());
        }
      }
      return _results;
    };

    Hem.prototype.test = function() {
      var targets, testOptions;
      targets = argv.targets;
      testOptions = {
        basePath: this.homeDir
      };
      if (argv.watch) {
        this.watch();
        testOptions.singleRun = false;
      } else {
        this.buildTargets(targets);
        testOptions.singleRun = true;
      }
      return this.testTargets(targets, testOptions);
    };

    Hem.prototype.check = function() {
      var app, inspect, printOptions, targetAll, targets, _i, _len, _ref, _ref1, _results;
      printOptions = {
        showHidden: false,
        colors: !argv.nocolors,
        depth: null
      };
      inspect = require('util').inspect;
      utils.log("> Configuration for <green>hem</green>:");
      console.log(inspect(this.options.hem, printOptions));
      utils.log("");
      targets = argv.targets;
      targetAll = targets.length === 0;
      _ref = this.apps;
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        app = _ref[_i];
        if (!((_ref1 = app.name, __indexOf.call(targets, _ref1) >= 0) || targetAll)) {
          continue;
        }
        utils.log("> Configuration values for <green>" + app.name + "</green>:");
        console.log(inspect(app, printOptions));
        _results.push(utils.log(""));
      }
      return _results;
    };

    Hem.prototype.exec = function(command) {
      if (command == null) {
        command = argv.command;
      }
      if (!this[command]) {
        return help();
      }
      return this[command]();
    };

    Hem.prototype.readSlug = function(slug) {
      if (!(slug && fs.existsSync(slug))) {
        return {};
      }
      return JSON.parse(fs.readFileSync(slug, 'utf-8'));
    };

    Hem.prototype.getTargetApps = function(targets) {
      var app, targetAll, _i, _len, _ref, _ref1, _results;
      if (targets == null) {
        targets = [];
      }
      targetAll = targets.length === 0;
      _ref = this.apps;
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        app = _ref[_i];
        if ((_ref1 = app.name, __indexOf.call(targets, _ref1) >= 0) || targetAll) {
          _results.push(app);
        }
      }
      return _results;
    };

    Hem.prototype.testTargets = function(targets, options) {
      var app, testApps;
      if (targets == null) {
        targets = [];
      }
      if (options == null) {
        options = {};
      }
      testApps = (function() {
        var _i, _len, _ref, _results;
        _ref = this.getTargetApps(targets);
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          app = _ref[_i];
          if (app.test) {
            _results.push(app);
          }
        }
        return _results;
      }).call(this);
      return testing.run(testApps, options);
    };

    Hem.prototype.buildTargets = function(targets) {
      var app, _i, _len, _ref, _results;
      if (targets == null) {
        targets = [];
      }
      _ref = this.getTargetApps(targets);
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        app = _ref[_i];
        _results.push(app.build());
      }
      return _results;
    };

    Hem.prototype.versionTargets = function(targets) {
      var app, _i, _len, _ref, _results;
      if (targets == null) {
        targets = [];
      }
      _ref = this.getTargetApps(targets);
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        app = _ref[_i];
        _results.push(app.version());
      }
      return _results;
    };

    return Hem;

  })();

  module.exports = Hem;

}).call(this);
